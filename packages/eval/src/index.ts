// @airun/eval — eval primitive and runner (planned).
//
// Runs a node or workflow against a dataset of test cases and scores it
// (LLM-as-judge or assertions). Pairs with the evaluator-optimizer pattern.
// Implementation not started yet.

export {};
