// @airun/node-registry — the canonical list of node types.
//
// Each entry carries category, friendly name + technical type, port geometry,
// icon, default config and config schema. Adding a node type starts here
// (registry entry + emitter in @airun/compiler + inspector form in the UI).
// Implementation not started yet.

export {};
