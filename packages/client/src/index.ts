// @airun/client — typed client for the runtime / cloud API.
//
// Trigger runs, fetch run history, and stream live execution traces from the
// step journal. Consumed by the builder's live-run overlay and the dashboard.
// Implementation not started yet.

export {};
