// The drag-and-drop payload key shared by the palette (sets it on dragstart) and
// the canvas (reads it on drop) so a palette item becomes a node.
export const NODE_DND_MIME = "application/airun-node-type";
