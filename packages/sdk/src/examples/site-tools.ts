// Developer-owned fn-tool handlers referenced by landing.generated.ts.
// In a real project these would live in the user's codebase; here they are
// stubs so the generated workflow's `import { ... } from "./site-tools.js"`
// resolves and typechecks.

export function writeComponentHandler(args: unknown): unknown {
  return args;
}

export function lintCheckHandler(args: unknown): unknown {
  return args;
}

export function finalizeSiteHandler(args: unknown): unknown {
  return args;
}
