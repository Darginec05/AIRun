// Developer-owned fn-tool handlers referenced by crm-assistant.generated.ts.
// In a real project these would live in the user's codebase; here they are
// stubs so the generated workflow's `import { ... } from "./crm-tools.js"`
// resolves and typechecks.

export function searchUnitsHandler(args: unknown): unknown {
  return args;
}
