#!/usr/bin/env node
// @airun/cli — the `airun` command.
//
// Compile *.graph.json → owned TypeScript in CI, run a workflow locally against
// @airun/runtime, and deploy. Makes the project dev-team-real, not just a
// playground. Implementation not started yet.

export {};
