# @airun/dashboard

Observability app: run history, traces, step timings, retries, payloads — the
trigger.dev-style operator view, reading off the `@airun/runtime` step journal
via `@airun/client`.

Not implemented yet — scaffold only.
