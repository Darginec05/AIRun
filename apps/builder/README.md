# @airun/builder

Standalone web app (planned: Vite + React) that wraps `@airun/flow-builder` into
the full-viewport three-zone experience, loads the demo workflows, and connects
live-run to `@airun/runtime` via `@airun/client`.

Not implemented yet — scaffold only.
